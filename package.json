{
  "name": "distributed-job-queue",
  "version": "2.0.0",
  "description": "Production-grade distributed job queue with Redis, priority scheduling, retry backoff, DLQ, and visibility-timeout recovery",
  "main": "index.js",
  "scripts": {
    "start":   "node index.js",
    "dev":     "node --watch index.js",
    "test":    "jest --runInBand --forceExit",
    "lint":    "eslint src/ index.js"
  },
  "keywords": [
    "job-queue",
    "redis",
    "distributed",
    "worker",
    "retry",
    "dead-letter-queue",
    "priority-queue",
    "visibility-timeout"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "ioredis": "^5.3.2",
    "uuid":    "^9.0.0"
  },
  "devDependencies": {
    "jest":    "^29.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
